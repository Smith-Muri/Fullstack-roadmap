// write a js function that returns all the properties names of an Object

let nombres = {
    name: "Alison " + " camilo " + " andres "
};

function imprimirNombres(name){
    console.log(nombres.name);
}

imprimirNombres(nombres);